<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="moims_reply">
	<select id="findLatest" resultType="data.Moim">
		
		</select>
	<insert id="create" parameterType="java.lang.String">
		INSERT INTO REPLYS(ID, MOIM_ID, WRITER, COMENT) VALUES(REPLYS_SEQ.NEXTVAL, #{moimId}, #{writer}, #{comment},  )
	</insert>
	
	<select id="findById" resultType="data.Moim" parameterType="string">
		 SELECT * FROM REPLYS WHERE MOIM_ID=#{moimId} OREDER BY WRITED ASC
	</select>
</mapper>
